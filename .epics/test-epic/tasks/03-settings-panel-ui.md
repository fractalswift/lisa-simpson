# Task 03: Settings Panel UI

## Status: done

## Goal
Create the SettingsPanel component with all form controls for theme, font size, units, default region, and weather field visibility toggles.

## Files
- weather-app/src/components/SettingsPanel.tsx

## Steps
1. Create SettingsPanel component with TypeScript props
2. Implement theme toggle (dark/light mode switch)
3. Add font size slider control with visual feedback
4. Create unit toggles for temperature (°C/°F) and wind speed (mph/kph)
5. Add default region input field
6. Implement weather field checkboxes for visibility (temp, humidity, wind, UV, pressure, visibility)
7. Style with Tailwind CSS following existing patterns

## Done When
- [ ] SettingsPanel renders all controls correctly
- [ ] Theme toggle switches between dark/light modes
- [ ] Font size slider adjusts and shows current value
- [ ] Unit toggles work for temperature and wind speed
- [ ] Default region field accepts and stores input
- [ ] Weather field checkboxes toggle individual fields
- [ ] Component matches existing styling patterns

## Notes
Use existing Tailwind classes and component structure. Ensure the panel is accessible and user-friendly.